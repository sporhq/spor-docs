---
title: Concepts
description: The graph model — typed nodes, typed edges, and the loop that keeps context alive.
sidebar:
  order: 1
---

:::note
This section is under construction. It will cover the node model, every node
and edge type, the decision queue and its ranking signals, briefings and
compilation, schemas as data, identity and attribution, claims, dispatch,
lenses, workflows, and capture.
:::

Spor is a typed, versioned knowledge graph of the durable outcomes of work.
Each node is a markdown file recording one fact — a decision, a task, an
issue, a norm, a question — and typed, directional edges carry the lineage a
flat list can't: why a node exists and what it depends on.
